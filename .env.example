# Copy this file to ".env" on your own machine for optional advanced features (e.g. public API).
# Only variables prefixed EXPO_PUBLIC_ are bundled into the Expo client — never put private keys here.
#
# Example (replace with a real key locally; do not commit .env):
# EXPO_PUBLIC_DEMO_API_KEY=
